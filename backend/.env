# Twelve Data API Key (NEVER commit the real key)
TWELVE_DATA_API_KEY='c04c9c4a7e6845f380c5e60cf59852ff'

# Optional: restrict client connections with a simple shared token
# Leave empty to allow all connections (for local testing)
CLIENT_TOKEN=

# Server
HOST=0.0.0.0
PORT=8000
LOG_LEVEL=INFO

# CORS origins (comma-separated). Use * only for local dev.
CORS_ORIGINS=*

# Max simultaneous mobile WebSocket clients
MAX_CLIENTS=300
